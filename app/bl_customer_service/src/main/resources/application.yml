spring:
  application:
    name: bl_customer_service
  datasource:
    url: jdbc:postgresql://localhost:5432/customer_db
    username: user
    password: user
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
server:
  port: 8083
