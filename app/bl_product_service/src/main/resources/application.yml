spring:
  application:
    name: bl_product_service
  datasource:
    url: jdbc:postgresql://localhost:5432/store
    username: user
    password: user
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 8081